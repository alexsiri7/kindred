# Railway config for the web service.
# Dashboard settings required: Root Directory = "/", Config-as-code Path = "web/railway.toml".
# Build context is the repo root so COPY lib/ can reach the shared lib package.

[build]
dockerfilePath = "web/Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 60
